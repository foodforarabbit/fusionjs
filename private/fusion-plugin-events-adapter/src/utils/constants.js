// @flow
/*
 NOTE: should not be kept in lockstep with actual package version—
 any resemblance to package version is coincidental.

 Only update this version if there is a significant change to how we publish M3 routing metics.
 Every time this number gets bumped services get an entirely new timeseries created
*/
const M3_ROUTE_METRICS_VERSION = '3.0.0';
export {M3_ROUTE_METRICS_VERSION};
