/* @noflow */

import styletronConstants from 'styletron-legacy/constants';

const {INSTANCE_KEY} = styletronConstants;

export {INSTANCE_KEY};

export const HYDRATION_ID = '_fusion_legacy_styling_hydration_keys_';
